apiVersion: apps/v1
kind: Deployment
metadata:
  name: concierge
spec:
  template:
    spec:
      # KinD and Docker bridge are separate networks (172.20.x vs 172.17.x)
      # hostNetwork lets the pod reach Ollama on localhost
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
