apiVersion: v1
kind: Service
metadata:
  name: sundae-mcp
  namespace: demo
  labels:
    app.kubernetes.io/name: sundae-mcp
    app.kubernetes.io/component: mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sundae-mcp
  ports:
    - name: http
      port: 8101
      targetPort: http
