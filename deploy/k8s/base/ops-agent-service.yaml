apiVersion: v1
kind: Service
metadata:
  name: ops-agent
  namespace: sundae-funday
  labels:
    app.kubernetes.io/name: ops-agent
    app.kubernetes.io/component: a2a
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ops-agent
  ports:
    - name: http
      port: 8202
      targetPort: http
